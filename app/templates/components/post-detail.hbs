{{yield}}
<div class="wrapper">
  <div class="block">
    <div class="post">
      <h1 class="post--title">{{post.title}}</h1>
      <span class="post--meta post--date">{{post.shortDate}}</span>
      <div class="post--content">{{post.contentRendered}}</div>
      <div class="post--content timebomb">{{post.date}}<br><br><pre class="content">{{post.contentMarkdown}}</pre></div>
    </div>
  </div>
</div>
